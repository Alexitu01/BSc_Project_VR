# requirements-sharp.txt
# Install in the 'sharp' conda environment
# Used by: RunSharp.py
#
# Install PyTorch first (not listed here as it requires --index-url):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install ml-sharp itself:
#   cd ml-sharp && pip install -e .
#
# Then install these:

pillow
numpy
piexif